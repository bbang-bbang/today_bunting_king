# 내일은 번트왕 — 사용법 가이드

> 텔레그램 봇 기반 국내 주식 주간스윙 추천·실행 플랫폼.
> 시드 100만원 (모의투자 시 최대 1,000만원), 번트/스퀴즈 2모드.

---

## 1. 한눈에 보는 주간 흐름

```
일 23:00    분석 유니버스 재빌드 (시총 top 500 + 거래대금 10억+)
월 08:30    추천 5종목 자동 푸시 → [매수/건너뜀/취소] 버튼
월~금 장중   TP/SL 가격 모니터 (3분 간격, 도달 시 알림)
금 15:20    미청산 포지션 청산 리마인더 (매도사유 태그)
금 15:40    주간 회고 리포트 (추천 N · 매수 N · 승률 · 실현손익)
```

매일 앱을 볼 필요 없고, **월요일에 들어와서 매수 결정 → 금요일까지 알림만 받으면 됩니다.**

---

## 2. 처음 등록하기

```
/start <초대코드>
```

- `.env` 의 `TELEGRAM_INVITE_CODE` 값을 전달
- 등록 후 관리자 승인 대기 (`status = pending → approved`)
- 승인되면 `/잔고` 로 초기 상태 확인 가능

### PIN 설정 (선택)
```
/setpin <6자리 숫자>
```
- 시드의 30% 초과 주문 시 PIN 확인 요구 (가드 정책)
- 평상시 번트 스윙은 1종목 ≤ 시드의 10%라 PIN 불필요

---

## 3. 명령어 레퍼런스

| 명령어 | 한글 단축 | 설명 |
|---|---|---|
| `/recommend` | `추천` / `종목추천` / `오늘추천` | 유니버스 500종목에서 상위 5종목 추천 |
| `/balance` | `잔고` / `내잔고` / `포지션` | 봇 DB 기준 + KIS 실계좌 잔고 병행 표시 |
| `/mode bunt\|squeeze` | `모드 번트` / `모드 스퀴즈` | 전략 모드 전환 |
| `/panic` | `긴급청산` / `전체매도` / `패닉` | 전 포지션 시장가 즉시 청산 |
| `/help` | `도움` / `도움말` / `명령어` | 명령어 목록 |
| `/setpin <6자리>` | — | PIN 설정 |

> Telegram 제약으로 슬래시 명령어는 영문 필수. 한글은 일반 메시지로 입력.

---

## 3.5 추천 캐시 정책

- `/추천` 첫 호출 시 500종목 평가 → **약 2~3분 소요** (진행 메시지 표시됨)
- 계산 결과는 `recommendations` 테이블에 저장
- **같은 날 재요청 시 캐시 반환 → 즉시 (0.5초)**, 메시지 헤더에 `📌 오늘 추천 (캐시, …)` 표기
- 월 08:30 자동 추천도 같은 DB 에 저장 → 이후 수동 /추천 시 즉시 반환
- **다음 거래일부터 새 캐시** (session_date 다르면 자동 재계산)

## 4. 추천 메시지 읽는 법

```
[KR-20260422-01] 삼성전자 (005930)
앙상블 점수: 72.3/100  (bunt)
업종: 반도체
시총 127.2조 · PER 12.4 · ROE 8.5%

진입 72,000 × 5주 = 360,000원
목표 77,040 (+7.0%) / 손절 69,120

최근 5일: 68,000 → 69,500 → 70,000 → 71,200 → 72,000 (+5.9%)

최근 뉴스:
  · HBM4 양산 돌입… 메모리 업사이클 재진입
  · 3Q 실적 컨센서스 상회 전망

10분 내 결정 (만료 시 /추천 재입력)
[매수]  [건너뜀]  [취소]
```

### 각 필드 의미

| 필드 | 의미 |
|---|---|
| `KR-20260422-01` | 추천 ID (시장-세션일자-순번). 회고 리포트와 매칭 |
| 앙상블 점수 | 7명의 전문가 가중합 (0~100). **50점 이상만 추천** |
| (bunt) | 사용자 현재 모드 (번트 or 스퀴즈) |
| 진입 × 수량 | 현재가 × 시드 10% 이내 자동 계산된 주문 수량 |
| 목표 / 손절 | 번트 +7%/-4%, 스퀴즈 +12%/-5% (스윙 파라미터) |
| 최근 5일 | 직전 주 일봉 추세 (상승 중인지 확인용) |
| 최근 뉴스 | 최근 1일 기사 최대 3건 (감성분석 반영됨) |
| 10분 내 결정 | 버튼 TTL — 만료 시 /추천 재입력으로 캐시에서 새 UUID 재발급 |

### 7명의 전문가 가중치

| 전문가 | 번트 | 스퀴즈 | 보는 것 |
|---|---|---|---|
| 기술 | **33%** | **37%** | SMA·EMA·MACD·RSI·BB·OBV·거래량 |
| 재무 | 18% | 9% | PER·PBR·ROE·시총·부채비율 |
| 흐름 | 16% | 16% | 외인·기관 5일 순매수 |
| 뉴스 | 9% | 9% | 최근 7일 기사 감성·활성도 |
| 분봉 | 9% | 13% | 당일 VWAP·거래량 급증·눌림목 |
| 커뮤니티 | 8% | 8% | 네이버 종목토론·StockPlus |
| 유튜브 | 7% | 8% | 조회수 가중 감성 |

번트는 **재무 비중↑ (안정성)**, 스퀴즈는 **기술·분봉 비중↑ (돌파 타이밍)**.

---

## 5. 매수 플로우

1. **[매수]** 버튼 클릭 (10분 내, 만료 시 `/추천` 재입력으로 갱신 가능)
2. 리스크 가드 통과 확인 (시드 한도, 중복 보유 금지 등)
3. KIS API 로 지정가 주문 접수
4. 체결 성공 → DB 에 `positions.status=open` 기록 + 사유 `trust_ensemble` 자동 기록
5. 주문 후 잔고 스냅샷 표시 (태그 선택 UI 없음)

### 체결 후 표시 예
```
✅ 매수 체결
005930  5주 @ 72,000원
수수료 54원 · 총 360,054원
목표 77,040 / 손절 69,120

▶ 주문 후 가용 현금: 9,639,946원
▶ 보유 포지션: 1종목
▶ KIS 실계좌 주문가능현금: 9,639,946원
```

> 매수 사유는 "앙상블 신뢰"로 자동 기록됩니다 — 추천 보고 매수한 것이니 선택 UI 없음.

---

## 6. 건너뜀 플로우

1. **[건너뜀]** 버튼 클릭
2. 건너뜀 사유 태그 키보드:
    - ❓ 신뢰 낮음
    - 💸 현금 부족
    - ⏰ 타이밍 놓침
    - · 기타
3. 태그 선택 → 기록 (매수하지 않음)
4. 금요일 회고에 "매수했으면 +X% 였음" 가정 표기

---

## 7. 매도 / 청산

### 자동 TP/SL 알림 (장중 3분마다)
```
🎯 목표 도달: 삼성전자 (005930)
  현재가 77,100원 (+7.1%)
  매수가 72,000원 × 5주
  예상 실현 손익: +25,500원
```
- 알림만, **자동 체결은 하지 않음**
- 사용자가 `/잔고` 확인 후 수동으로 매도하거나, 금요일 리마인더 대기

### 금요일 장마감 직전 리마인더 (15:20)
```
⏰ 금요일 장 마감 10분 전 — 주말 전 청산 필수!
  • [KR-20260420-01] 005930 5주 @72,000  목표 77,040 / 손절 69,120
  • [KR-20260420-03] 000660 2주 @120,000 목표 128,400 / 손절 115,200
매도 사유를 선택하세요 (종목별 개별 기록):
```
- 종목별로 매도사유 태그 키보드 표시 (6종):
  - 🎯 목표 도달
  - 🛑 손절 도달
  - ⏰ 주말 강제
  - ⚡ 충동매도
  - 📰 뉴스 변화
  - · 기타

### 긴급청산
```
/panic  또는  긴급청산
```
- 전 포지션 시장가 즉시 청산
- 실전 사고 대응용 — 평상시 사용 지양

---

## 8. 잔고 확인

`/잔고` 입력 시:

```
[잔고] kis_mock · bunt

── 봇 기준 (DB) ──
활성 시드:   10,000,000원
가용 현금:   9,639,946원
누적 손익:   +0원
봇 포지션:   1종목
  • 005930 5주 @72,000  목표 77,040 / 손절 69,120  (bunt)

── KIS kis_mock 실계좌 ──
주문가능현금: 9,639,946원
총평가금액:   360,500원
총손익:       +500원 (+0.14%)
보유 종목:    1종목
  • 005930 삼성전자 5주 @72,000 → 72,100 (+500원 / +0.14%)
```

- **봇 기준 (DB)**: 봇이 체결한 주문 기반 집계
- **KIS 실계좌**: 실제 브로커 API 로 당일 조회 — 봇 외 수동 거래 포함

두 값이 다르면 수동 개입(봇 외 주문)이 있었다는 뜻.

---

## 9. 모드 전환

### 번트 모드 (기본 — 안정)
- TP **+7%** / SL **-4%**
- 재무 비중 18% (안정적 종목 선호)
- 평균 보유일 2~3일 (월~목 내 목표 도달 시 조기 청산)

### 스퀴즈 모드 (공격)
- TP **+12%** / SL **-5%**
- 기술/분봉 비중↑ (BB 수축 + 거래량 급증 돌파 종목)
- 번트보다 리스크 큼 — 시드 여유 필요

전환:
```
/mode bunt    또는  모드 번트
/mode squeeze 또는  모드 스퀴즈
```

---

## 10. 리스크 가드 (자동 차단)

아래 조건 만족 시 봇이 매수를 **자동 거부**:

| 조건 | 설명 |
|---|---|
| 시드 상한 초과 | 시드 × 위치당 상한(%) 초과 주문 거부 |
| 현금 부족 | 가용현금 < 주문금액 |
| 중복 보유 | 동일 종목 이미 보유 중 |
| 관리종목 | 재무 `is_acceptable=False` |
| 거래정지 | KIS `is_tradable()=False` |
| 연속 손실일 | 5일 연속 손실 시 추가 진입 차단 |

PIN 요구 케이스:
- 주문금액 > 시드의 30% (번트 스윙에선 거의 없음)

---

## 11. 주간 회고 리포트 (금 15:40)

```
📊 [2026-04-24] 회고 리포트

🟢 [KR-20260420-01] 삼성전자(005930)
   진입 72,000 → 종가 76,800  |  +23,500원
   매수사유: 앙상블 신뢰  매도사유: 목표 도달

⏭ [KR-20260420-02] SK하이닉스(000660)
   건너뜀 (신뢰 낮음)  (매수했으면 +3.21%)

⬜ [KR-20260420-03] LG에너지솔루션(373220)  미응답  (종가 기준 -1.10%)

추천 3 | 매수 1 | 건너뜀 2
실현 손익: +23,500원 | 승률: 100%
```

### 해석 팁
- **🟢 매수 성공** + 수익률 → 좋은 판단
- **⏭ 건너뜀** + "매수했으면 +X%" → 놓친 기회 가늠
- **⬜ 미응답** → 추천 무시 (버튼도 안 누름) → 피드백 루프용

---

## 12. 운영자 수동 명령 (터미널)

### 유니버스 수동 재빌드
```bash
python -m src.universe.builder
# 또는 기준일 지정:
python -m src.universe.builder --as-of 2026-04-22
```
- 주 1회 일요일 23:00 자동 실행되지만, 장애 시 수동 실행 가능

### 데이터 수집
```bash
# 첫 실행 (3년 백필, 수 시간)
python -m src.crawlers.collect_all --first-time

# 일일 증분 (유니버스 기준 500종목만, ~1시간)
python -m src.crawlers.collect_all --daily --continue-on-error
```

### 봇 기동
```bash
python -m src.bot.telegram_bot
```

### 전체 테스트
```bash
python -m pytest tests/ -q
```

---

## 13. 문제 해결

### 추천 "0종목" 응답
- 유니버스가 비어있을 수 있음 → `python -m src.universe.builder`
- 점수 50 미달 or 시드 내 매수 불가 → `/mode squeeze` 로 전환해 재시도

### KIS 잔고 조회 실패
- 토큰 만료 가능 → 봇 재시작 시 새 토큰 발급
- 키 오류 시 `.env` 의 `KIS_MOCK_APP_KEY/SECRET/ACCOUNT_NO` 확인

### 봇이 응답 없음
- 프로세스 확인: `tasklist | grep python`
- 로그 확인: `logs/bot_v2.log`
- 재기동: `python -m src.bot.telegram_bot`

### 실전 계좌 전환
- 기본 금지 — 사용자 명시 허락 후에만
- `.env` 에 `TRADE_MODE=live` + `KIS_LIVE_*` 키 설정 필요
- **시드 상한 100만원** 절대 준수

---

## 14. 불변 규칙 (바꾸지 말 것)

1. **시드 상한 100만원** (모의투자 확장 상한 1,000만원)
2. **당일 청산 원칙** (국내 스윙은 월~금 내 청산, 오버주말 금지)
3. **번트 ≤ 스퀴즈** — 스퀴즈는 번트보다 리스크 큼
4. **실전 계좌 전환은 명시 허락 후에만**
5. **테스트 주문 금지** — 분석 파이프라인이 뽑은 실제 종목만 체결
6. **크롤링 = 분석 = 추천 동일 유니버스** — `analysis_universe` 테이블 공유

---

## 15. 파일 구조 (참고)

```
src/
  adapters/      broker_paper, broker_kis, market_data_*
  bot/           telegram_bot, scheduler (job 6종)
  crawlers/      collect_all + 단발/per-code 크롤러
  db/            schema.sql, connection.py
  ensemble/      scorer (7명 가중합), recommender (top N)
  experts/       technical, fundamental, flow, news, minute, community, youtube
  indicators/    SMA, EMA, MACD, RSI, BB, ATR, OBV, MFI, 이치모쿠
  risk/          guard (리스크 가드 — 유일 주문 통로)
  services/      portfolio, recommendation, confirmation, user, audit, price_monitor
  universe/      builder (top 500 + ADV 필터)

tests/           pytest 전체 (212개)
data/bunting.db  SQLite
logs/            봇·크롤러 로그
.env             KIS 키·텔레그램 토큰
```
